const initialState = {
  psikologs: [],
};

const psikologsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default psikologsReducer;
